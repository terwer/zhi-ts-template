{
  "name": "{{name}}",
  "version": "0.1.0",
  "type": "module",
  "description": "{{description}}",
  "scripts": {
    "dev": "zhi-build --watch",
    "build": "zhi-build"
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "rollup-config-custom": "workspace:*"
  }
}